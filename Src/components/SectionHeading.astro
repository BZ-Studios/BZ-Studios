---
interface Props {
  eyebrow: string;
  title: string;
  text?: string;
}
const { eyebrow, title, text } = Astro.props;
---

<div class="section-heading">
  <p class="kicker">{eyebrow}</p>
  <h2>{title}</h2>
  {text && <p>{text}</p>}
</div>
